/**
 * In a real situation I would look first if an existing module exists already. If this module is of
 * high quality, tested, maintained, and popular enough, I would just use it instead of re-inventing
 * the wheel.
 */
const { object } = require('dot-object');

module.exports = object;
